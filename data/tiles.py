"""Phase 2: Tile loading (stub for MVP)."""

from __future__ import annotations

from pathlib import Path


def load_tile(path: str | Path) -> dict:
    """Placeholder for loading preprocessed world tiles (npz format)."""
    raise NotImplementedError(
        "Tile loading is a phase 2 feature. "
        "Use procedural world generation for MVP."
    )
